/* function toggleOpecoes() {
    const bloco = document.getElementById("bloco-opecoes");
    const botao = event.target;

    const mostrando = bloco.style.display === "block";
    bloco.style.display = mostrando ? "none" : "block";
    botao.textContent = mostrando ? "Mais informações" : "Menos informações";
} */